import Footer from '.'
import type { Meta } from '@storybook/react'

const meta: Meta = {
  component: Footer,
  title: 'components/Footer',
}

export const Default = (): JSX.Element => <Footer />

export default meta
